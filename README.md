Read PostgreSQL using C++ and libpqxx 
===
A couple of C++ programs to access a PostgreSQL Server database using the libpqxx library.

Note: You need to install the following package

apt install libpqxx-dev